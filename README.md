## JsonViewLite
JSON viewer based on [React JSON view lite](https://www.npmjs.com/package/react-json-view-lite)

## Features
- View JSON formatted in human readable way
- Expand/collapse all
- Light and dark theme

## Usage
- Place the widget on a page and connect the attribute with the JSON content
- Optionally configure the expand/collapse and theme properties as desired
